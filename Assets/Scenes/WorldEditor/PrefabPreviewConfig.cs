using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PrefabPreviewConfig : MonoBehaviour
{
    public Quaternion cameraAngle = Quaternion.Euler(45f, 0, 0);
	public Vector3 cameraOffset = new Vector3(0, 4, -2);
	public float cameraZoom = 2f;


	// Start is called before the first frame update
	void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
